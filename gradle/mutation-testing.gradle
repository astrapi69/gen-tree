dependencies {
    pitest libs.pitest.testng.plugin
}

pitest {
    testPlugin = "testng"
    pitestVersion = libs.versions.pitest.version.get()
    targetClasses = ["io.github.astrapi69.gen.tree.*"]
    threads = 4
    outputFormats = ["HTML"]
    timestampedReports = false
}
